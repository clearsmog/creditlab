"""External benchmarks for CreditLab models (agency ratings, market data).

Import from the submodule directly (keeps ``python -m
creditlab.validation.agency`` clean of double-import warnings):

    from creditlab.validation.agency import compare_ratings
"""
